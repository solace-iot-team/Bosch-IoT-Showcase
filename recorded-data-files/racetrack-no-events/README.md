# Record Data

## Scenario: race track no-events
two cars on race track
one faster than the other
regular operations - no crashes or stop/start

## Goal
detect laps with gyroscope data - left/right bend, up/down bridge
detect laps with light - dark under bridge
